id: ISS-461
title: [CRITICAL] Weird crashing issue? need help indentifying
source: issue #461 (CLOSED, stateReason COMPLETED) https://github.com/ladybug-me/caelestia-kde/issues/461
tags: random apps crash, chromium, spotify, discord, steam, memory usage, live previews, systemd-cgtop
synonyms: apps randomly crash, browsers crash after caelestia, memory leak quickshell

# ISS-461 — [CRITICAL] Weird crashing issue? need help indentifying

State: CLOSED, stateReason COMPLETED · opened 2026-08-12 by Kyedae.

## Problem (original report, condensed)

For some reason when im doing random things sometimes random apps crash, they primarily seem to be chromium apps, Helium, Spotify, Discord, Steam, they sometimes randomly crash when clicking or just interacting with things, its difficult to reproduce on the spot, but when i had codex scan my system for the crash issue it seems to be involving Caelestia and the apps are victims, ill attach images with information

Latest Stable

CachyOS

[image]

[image]

## Resolution

Closed by PR #462 (MERGED), #496 (MERGED).
What maintainers/collaborators actually said (verbatim, lightly trimmed):

> **0xSolanaceae** (COLLABORATOR, 2026-08-12):
> @Kyedae could you provide the crash logs?

> **0xSolanaceae** (COLLABORATOR, 2026-08-12):
> Working on it now, will be in dev soon

> **0xSolanaceae** (COLLABORATOR, 2026-08-12):
> @Kyedae can you run this after caelestia has been running for a while:
>
> `systemd-cgtop -m
> ps -eo pid,ppid,rss,cmd --sort=-rss | head -25
> grep -E 'VmRSS|VmHWM|Threads' /proc/$(pgrep -n quickshell)/status
> grep -E 'Rss|Pss|Private' /proc/$(pgrep -n quickshell)/smaps_rollup
> systemctl show user@$(id -u).service -p ControlGroup -p MemoryCurrent -p MemoryPeak -p TasksCurrent`
>
> And then disable live previews and the close the overview and compare memory usage:
>
> `systemctl --user restart quickshell`
> `watch -n 5 "grep -E 'VmRSS|VmHWM|Threads' /proc/\$(pgrep -n quickshell)/status"`

> **0xSolanaceae** (COLLABORATOR, 2026-08-12):
> oops, forgot to enter in newlines:
> `pid=$(pgrep -n quickshell) && echo "PID=$pid" && ps -p "$pid" -o pid,ppid,rss,vsz,cmd && grep -E 'VmRSS|VmHWM|Threads' "/proc/$pid/status" && grep -E 'Rss|Pss|Private' "/proc/$pid/smaps_rollup" && systemctl show "user@$(id -u).service" -p MemoryCurrent -p MemoryPeak -p TasksCurrent`

> **0xSolanaceae** (COLLABORATOR, 2026-08-12):
> can you run it in bash/zsh? idk fish syntax

> **0xSolanaceae** (COLLABORATOR, 2026-08-12):
> yeah keep it off, restart quickshell and run it again please:
> `systemctl --user restart quickshell`

## Comment log

- **0xSolanaceae** (COLLABORATOR, 2026-08-12 01:14):
  @Kyedae could you provide the crash logs?

- **Kyedae** (NONE, 2026-08-12 01:38):
  > [@Kyedae](https://github.com/Kyedae) could you provide the crash logs?

  [=== Crash report D-Bus session ==.txt]([attachment]
  this is the file generated from codex

- **0xSolanaceae** (COLLABORATOR, 2026-08-12 01:46):
  Working on it now, will be in dev soon

- **0xSolanaceae** (COLLABORATOR, 2026-08-12 01:54):
  @Kyedae can you run this after caelestia has been running for a while:

  `systemd-cgtop -m
  ps -eo pid,ppid,rss,cmd --sort=-rss | head -25
  grep -E 'VmRSS|VmHWM|Threads' /proc/$(pgrep -n quickshell)/status
  grep -E 'Rss|Pss|Private' /proc/$(pgrep -n quickshell)/smaps_rollup
  systemctl show user@$(id -u).service -p ControlGroup -p MemoryCurrent -p MemoryPeak -p TasksCurrent`

  And then disable live previews and the close the overview and compare memory usage:

  `systemctl --user restart quickshell`
  `watch -n 5 "grep -E 'VmRSS|VmHWM|Threads' /proc/\$(pgrep -n quickshell)/status"`

- **Kyedae** (NONE, 2026-08-12 01:57):
  > [@Kyedae](https://github.com/Kyedae) can you run this after caelestia has been running for a while:
  >
  > `systemd-cgtop -m ps -eo pid,ppid,rss,cmd --sort=-rss | head -25 grep -E 'VmRSS|VmHWM|Threads' /proc/$(pgrep -n quickshell)/status grep -E 'Rss|Pss|Private' /proc/$(pgrep -n quickshell)/smaps_rollup systemctl show user@$(id -u).service -p ControlGroup -p MemoryCurrent -p MemoryPeak -p TasksCurrent`
  >
  > And then disable live previews and the close the overview and compare memory usage:
  >
  > `systemctl --user restart quickshell` `watch -n 5 "grep -E 'VmRSS|VmHWM|Threads' /proc/\$(pgrep -n quickshell)/status"`

  [image]
  also live previews have been off and overview is off

- **0xSolanaceae** (COLLABORATOR, 2026-08-12 01:59):
  oops, forgot to enter in newlines:
  `pid=$(pgrep -n quickshell) && echo "PID=$pid" && ps -p "$pid" -o pid,ppid,rss,vsz,cmd && grep -E 'VmRSS|VmHWM|Threads' "/proc/$pid/status" && grep -E 'Rss|Pss|Private' "/proc/$pid/smaps_rollup" && systemctl show "user@$(id -u).service" -p MemoryCurrent -p MemoryPeak -p TasksCurrent`

- **Kyedae** (NONE, 2026-08-12 02:00):
  > oops, forgot to enter in newlines: `pid=$(pgrep -n quickshell) && echo "PID=$pid" && ps -p "$pid" -o pid,ppid,rss,vsz,cmd && grep -E 'VmRSS|VmHWM|Threads' "/proc/$pid/status" && grep -E 'Rss|Pss|Private' "/proc/$pid/smaps_rollup" && systemctl show "user@$(id -u).service" -p MemoryCurrent -p MemoryPeak -p TasksCurrent`

  [image]

- **Kyedae** (NONE, 2026-08-12 02:00):
  i did set aswell btw and didnt work

- **0xSolanaceae** (COLLABORATOR, 2026-08-12 02:01):
  can you run it in bash/zsh? idk fish syntax

- **Kyedae** (NONE, 2026-08-12 02:03):
  [image]

  I already had previews and overview off though should i keep it off

- **0xSolanaceae** (COLLABORATOR, 2026-08-12 02:05):
  yeah keep it off, restart quickshell and run it again please:
  `systemctl --user restart quickshell`
